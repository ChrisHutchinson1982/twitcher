import SearchBar from "../searchBar/searchBar";

const HomePage = () => {
  return (
    <main id="main-container">
      <div>
        <SearchBar />
      </div>
    </main>
  );
};

export default HomePage;
